# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files — never commit (OpenAI key + access code live here)
.env
.env.local
.env*.local

# secrets — never commit the OpenAI key
secret.key.json
*.key.json
secret.key
*.secret

# typescript
*.tsbuildinfo
next-env.d.ts.bak

# source catalog PDFs — never commit / never expose as a download (only cropped
# reference webp under public/catalog/ are committed). Drop PDFs in docs/ to import.
/docs/*.pdf
*.pdf

# raw extracted catalog images (source) — only the optimized webp in public/ are committed
/gym pics/
